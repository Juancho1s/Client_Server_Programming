<h1>
    <%= @articles.title%>
</h1>


<p>
    <%= @articles.body%>
</p>

<ul>
    <li>
        <%= link_to "Edit", edit_article_path(@articles) %>
    </li>
    <li>
        <%= link_to "Delete", article_path(@articles), data: {
            turbo_method: :delete,
            turbo_confirm: "Are you sure to delete this record?"
        } %>
    </li>
</ul>